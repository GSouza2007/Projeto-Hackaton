<main>
      <header>
      <!-- Redirecionar para o início ao clicar no ícone -->
      <a href="/" class="logo">
        <img src="../assets/logo/logo_portal_m.png" alt="Logo">
      </a>
      
      <!-- Navbar -->
    <nav class="navbar">
      <button class="hamburger"></button>
      <ul class="navbar-list list-unstyled d-flex justify-content-center justify-content-lg-start">
        <li><a href="comentariosAdicionar">Comentar</a></li>
        <li><a href="noticiasAdicionar">Notícias</a></li>
        <li><a href="eventosAdicionar">Publicar</a></li>
      </ul>
    </nav>
    </header>
    

    <div class="btn btn-warning eventos">
        <a href="eventosAdicionar">Adicionar evento</a>
    </div>
    {{!--Listagem de eventos / produtos --}}
    <div class="container">
        <div class="row">
           {{#each eventos}}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card">
                    <img src="../imagens/{{imagem}}">
                    <div class="textos">
                        <p>{{nome}}</p>
                        <p>R${{valor}}</p>
                    </div>
                    <a href="/eventosEditar/{{id}}" class="btn btn-warning">Alterar</a>
                    <a href="/remover/{{id}}&{{imagem}}" class="btn btn-danger">Remover</a>
                </div>
            </div>
           {{/each}}
        </div>
    </div>

    <hr>

    <div class="btn btn-warning acima">
        <a href="noticiasAdicionar">Adicionar noticia</a>
    </div>

    <div class="container">
        <div class="row">
           {{#each noticias}}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card">
                    <img src="../imagens/{{imagem}}">
                    <div class="textos">
                        <p>{{nome}}</p>
                        <p>{{descricao}}</p>
                        <p>{{autor}}</p>
                    </div>
                    <a href="/noticiasEditar/{{id}}" class="btn btn-warning">Alterar</a>
                    <a href="/removerNoticias/{{id}}&{{imagem}}" class="btn btn-danger">Remover</a>
                </div>
            </div>
           {{/each}}
        </div>
    </div>

     <div class="btn btn-warning acima">
        <a href="comentariosAdicionar">Adicionar comentário</a>
    </div>

    <div class="container">
        <div class="row">
           {{#each comentarios}}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card">
                    <div class="textos">
                        <p style="font-size: 20px;">'<strong>{{comentario}}</strong>'</p>
                        <p>-{{nome}}</p>
                    </div>
                    <a href="/removerComentarios/{{id}}" class="btn btn-danger">Remover</a>
                </div>
            </div>
           {{/each}}
        </div>
    </div>

    <br><br>

    <!-- Footer -->
    <footer class="footer pt-5 pb-3">
      <div class="container">
        <div class="row">
          <div class="col-12 col-md-4 mb-4">
            <svg class="portal-logo" width="150" height="50" viewBox="0 0 150 50">
              <text x="0" y="35" fill="white" font-size="16" font-weight="bold">PORTAL M NORTE</text>
            </svg>
            <p>Notícias atualizadas da comunidade.</p>
            <div class="social-links">
            </div>
          </div>

          <div class="footer-section col-12 col-md-4 mb-4">
            <h3>Links Rápidos</h3>
            <ul class="footer-links">
              <li><a href="#jogospopulares">🔔 Notícias Recentes</a></li>
              <li><a href="#promocoes">📝 Publicar Notícias</a></li>
              <li><a href="#jogosdestaques">📌 Recomendado</a></li>
              <li><a href="feedback">💬 Feed Back</a></li>
            </ul>
          </div>

        <div class="footer-section col-12 col-md-4 mb-4">
          <h3>Fique por dentro de todas as notícias!</h3>
        </div>
      

        <div class="footer-bottom row mt-4">
          <div class="col-12 text-center">
            <p class="mb-0">Criado por ... </p>
          </div>
        </div>
    </footer>

</main>